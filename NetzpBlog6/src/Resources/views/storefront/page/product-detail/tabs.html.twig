{% sw_extends '@Storefront/storefront/page/product-detail/tabs.html.twig' %}

{% block page_product_detail_tabs_navigation_review %}
    {{ parent() }}

    {% if(config('NetzpBlog6.config.showblogposts') and
        page.product.getExtension('blogs').getElements()) %}
    <li class="nav-item">
        <a class="nav-link product-detail-tab-navigation-link"
           id="netzp-blog-tab"
           data-toggle="tab"
           data-offcanvas-tabs="true"
           href="#netzp-blog-tab-pane"
           role="tab"
           aria-controls="netzp-blog-tab-pane"
           aria-selected="true">
            {{ "netzp.blog.tab-posts-title"|trans }}
            <span class="product-detail-tab-navigation-icon">
                {% sw_icon 'arrow-medium-right' style {'pack':'solid'} %}
            </span>
        </a>
    </li>
    {% endif %}
{% endblock %}

{% block page_product_detail_tabs_content_review %}
    {{ parent() }}

    {% if(config('NetzpBlog6.config.showblogposts') and
        page.product.getExtension('blogs').getElements()) %}
    <div class="tab-pane fade show"
         id="netzp-blog-tab-pane"
         role="tabpanel"
         aria-labelledby="netzp-blog-tab">

        {% sw_include "@Storefront/storefront/page/product-detail/tabs-blog-slider.html.twig"
            with {'posts': page.product.getExtension('blogs').getElements()}
        %}
    </div>
    {% endif %}
{% endblock %}
